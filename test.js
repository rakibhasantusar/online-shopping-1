// const rakibul = ['rak', 'bap', 'bap6y', 'bp', 'ap', 'ba']

// rakibul.forEach(r => console.log(r))

/* let shop = {}

shop[1] = 1

console.log(shop) */

const tusar = 'nice'
const number = [1, 35, 5, 74, 93, 22, 43, 62, 8, 10]


const main = number.findIndex(e => e === 74) // findIndex ta map er motoi jobbor

const every = number.every(e => e <= 93) // every ta prottek ta follow kore

const reduce = number.reduce((total, e) => total + e)// total ta chomotkar jinish aikhan a prothom a duita man ashe then tarpor total er man depend kore duitar jogfol er uportar por ja ghotar ghote
const arrayTusar = Array.from(tusar) // aita holo string dia array bananor ostro
console.log(arrayTusar)


